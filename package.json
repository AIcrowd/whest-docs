{
  "name": "whest-docs",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/site"
  ],
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "sync:sources": "node scripts/sync-sources.mjs",
    "prepare:content": "node scripts/prepare-content.mjs",
    "dev": "npm run dev --workspace @aicrowd/whest-docs-site",
    "build": "npm run build --workspace @aicrowd/whest-docs-site"
  }
}
